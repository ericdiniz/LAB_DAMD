// Include CocoaPods generated configurations so Xcode sandbox is in sync with Podfile.lock
// If CocoaPods generated the files, these includes will ensure the project's build configs
// reference the Pod xcconfig files. Paths are relative to this file's location.
// If the files do not exist yet, they will be ignored by Xcode until created by `pod install`.
#include "../../Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "../../Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "../../Pods/Target Support Files/Pods-Runner/Pods-Runner.profile.xcconfig"

// Default macOS deployment target for the app (can be overridden by Pod configs)
MACOSX_DEPLOYMENT_TARGET = 10.13

// Application-level settings for the Runner target.
// The application's name. By default this is also the title of the Flutter window.
PRODUCT_NAME = task_manager

// The application's bundle identifier
PRODUCT_BUNDLE_IDENTIFIER = com.example.taskManager

// The copyright displayed in application information
PRODUCT_COPYRIGHT = Copyright © 2025 com.example. All rights reserved.
